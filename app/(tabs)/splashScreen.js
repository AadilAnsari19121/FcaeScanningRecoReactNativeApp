import { Button, Image, View, Text, StyleSheet, Platform } from 'react-native';

export default SplashScreenNew = () => {
    return (
        <View style={{ backgroundColor: 'green', width: '100%', height: '100%', alignItems: 'center', justifyContent: 'center' }}>
            <Text style={{ color: 'blue', fontSize: 50.2 }}>This is text of splash screen</Text>
        </View>
    )
}